server:
   servlet:
    context-path: /
     
spring:
  application.name: springrestbootvault
  cloud.vault:
    scheme: http
    host: 127.0.0.1
    port: 8200
    authentication: token
    token: s.1h17gwH41iRHcUyntmeP7MU3
    kv:
      enabled: true
      default-context: secret
      application-name: springrestbootvault

logging:
  level:
    root: INFO
  pattern:
    console: "[%p] %d %c %M - %m%n"
