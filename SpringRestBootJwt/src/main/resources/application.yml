server:
   servlet:
    context-path: /
     
spring:
  application:
    name: springrest
 
jwt:
  header: Authorization
  secret: example-secret-key

logging:
  level:
    root: INFO
  pattern:
    console: "[%p] %d %c %M - %m%n"
