server:
   servlet:
    context-path: /
     
spring:
  application.name: springrestbootk8secret
  cloud:
    kubernetes:
      reload:
        enabled: true
      secrets:
        paths: /api/v1/namespaces/default/secrets/springrestbootk8secret

logging:
  level:
    root: INFO
  pattern:
    console: "[%p] %d %c %M - %m%n"
